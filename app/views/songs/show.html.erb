<h1><%= @song.name %></h1>

<h3><%= link_to @song.artist.name, artist_path(@song) %></h3>

<h3><%= link_to @song.genre.name, genre_path(@song) %></h3>

<p><%= link_to "Edit this song", edit_song_path(@song)%>

<p><%= link_to "See all songs", songs_path %></p>